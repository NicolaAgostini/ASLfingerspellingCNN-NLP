The test dataset is in Test folder, if you change the image it must have the same pattern of folders.

Use the dataset https://empslocal.ex.ac.uk/people/staff/np331/index.php?section=FingerSpellingDataset  5 users

Use preprocessing_dataset5() in main to generate the training, test and validation set, the others time you execute comment this function call.

Other tips in Final Report appendix "Instructions to execute the project"

The models are included (file json and h5)


If you want to use model with crop images just uncomment the function call in main file
